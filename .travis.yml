---

language: c
compiler: gcc

dist: trusty
sudo: required

before_install:
  - sudo apt-get update
  - make get-dep-debian-all

install:
  - make all TRAVIS=1
  - make install-all

script: true

notifications:
  email: false
